import React from 'react';
import ProductList from '../components/ProductList';

const InventoryPage = () => {
    return (
        <div>
            <ProductList />
        </div>
    );
};

export default InventoryPage;
